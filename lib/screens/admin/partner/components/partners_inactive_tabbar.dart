import 'package:flutter/material.dart';

class PartnersInactiveTabbar extends StatelessWidget {
  const PartnersInactiveTabbar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
